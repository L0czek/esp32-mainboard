# WiFi Station (STA) credentials - for connecting to existing WiFi network
# REQUIRED: These must be set as environment variables at build time
export WIFI_SSID=YourNetworkName
export WIFI_PASSWORD=YourNetworkPassword

# Access Point (AP) credentials - for the ESP32's own WiFi network
export AP_SSID=ESP32-AP
export AP_PASSWORD=password123
